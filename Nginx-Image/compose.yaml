services:
  nginx:
    build: .
    container_name: nginx-container
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
      - /usr/share/nginx/html/TestFolder

volumes:
  volume_test: